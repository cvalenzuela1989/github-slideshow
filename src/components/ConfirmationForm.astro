---
const formAction = "https://formsubmit.co/milagroramirez477@gmail.com";
const eventInfo = {
  date: "7 de Junio de 2025",
  time: "21:00",
  dressCode: "Vestimenta semiformal",
  location: "Dinastía Salón de Eventos"
};
---

<section class="confirmation-section">
  <div class="event-card">
    <div class="event-icon">🗓️</div>
    <div class="event-details">
      <p><strong>{eventInfo.date}</strong> a las <strong>{eventInfo.time}</strong></p>
      <p>{eventInfo.location}</p>
      <p class="dress-code">{eventInfo.dressCode}</p>
    </div>
  </div>

  <div class="confirmation-header">
    <h2 class="confirmation-title">Confirmar Asistencia</h2>
    <div class="deadline-badge">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-calendar">
        <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
        <line x1="16" y1="2" x2="16" y2="6"></line>
        <line x1="8" y1="2" x2="8" y2="6"></line>
        <line x1="3" y1="10" x2="21" y2="10"></line>
      </svg>
      <span>Confirma antes del 20 de mayo</span>
    </div>
  </div>
  
  <form action={formAction} method="POST" class="confirmation-form">
    <!-- Campos ocultos para FormSubmit -->
    <input type="hidden" name="_next" value="https://soymilayteinvitoamisxv.netlify.app/gracias">
    <input type="hidden" name="_subject" value="Confirmación XV Años - Nuevo RSVP">
    <input type="hidden" name="_template" value="table">
    <input type="text" name="_honey" style="display:none"> <!-- Campo honeypot para spam -->
    
    <div class="form-group">
      <label for="name">Nombre completo</label>
      <input type="text" id="name" name="Nombre" required placeholder="Ej: María González">
    </div>

    <div class="form-group">
      <label>¿Asistirás?</label>
      <div class="radio-group">
        <label class="radio-option">
          <input type="radio" name="Asistencia" value="Sí" required> 
          <span>Sí, con mucho gusto</span>
        </label>
        <label class="radio-option">
          <input type="radio" name="Asistencia" value="No"> 
          <span>No podré asistir</span>
        </label>
      </div>
    </div>

    <div class="form-group">
      <label for="guests">Número de invitados</label>
      <select id="guests" name="Invitados">
        <option value="1 persona">1 persona</option>
        </select>
    </div>

    <div class="form-group">
      <label for="message">Mensaje especial (opcional)</label>
      <textarea id="message" name="Mensaje" placeholder="Escribe un mensaje si lo deseas..."></textarea>
    </div>

    <button type="submit" class="submit-button">Enviar Confirmación</button>
  </form>
</section>

<style>
  .confirmation-section {
    max-width: 600px;
    margin: 2rem auto;
    padding: 1.5rem;
    background: white;
    border-radius: 15px;
    box-shadow: 0 5px 20px rgba(70, 130, 180, 0.1);
  }

  .confirmation-header {
    text-align: center;
    margin-bottom: 1.5rem;
  }
  
  .confirmation-title {
    font-size: 1.8rem;
    color: #4682B4;
    margin-bottom: 0.5rem;
    font-weight: 600;
    letter-spacing: 0.5px;
    position: relative;
    display: inline-block;
  }
  
  .confirmation-title:after {
    content: '';
    position: absolute;
    width: 50%;
    height: 3px;
    background: linear-gradient(to right, #B0E0E6, #4682B4);
    bottom: -8px;
    left: 25%;
    border-radius: 3px;
  }
  
  .deadline-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background-color: #E6F7FF;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.9rem;
    color: #4682B4;
    border: 1px solid #ADD8E6;
  }
  
  .deadline-badge svg {
    width: 14px;
    height: 14px;
  }

  .event-card {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: linear-gradient(135deg, #F0F8FF 0%, #E6F7FF 100%);
    padding: 1rem;
    border-radius: 12px;
    margin-bottom: 1.5rem;
    border-left: 4px solid #87CEEB;
    box-shadow: 0 2px 8px rgba(135, 206, 235, 0.1);
  }

  .event-icon {
    font-size: 1.8rem;
    color: #4682B4;
  }

  .event-details {
    flex: 1;
  }

  .event-details p {
    margin: 0.3rem 0;
    font-size: 0.95rem;
    color: #5F7D8B;
  }

  .dress-code {
    color: #5AA2D1;
    font-weight: 500;
  }

  .form-group {
    margin-bottom: 1.2rem;
  }

  label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 500;
    color: #5F7D8B;
  }

  input[type="text"],
  select,
  textarea {
    width: 100%;
    padding: 0.8rem;
    border: 1px solid #E6F7FF;
    border-radius: 8px;
    font-size: 1rem;
    background-color: #F8FDFF;
  }

  textarea {
    min-height: 100px;
    resize: vertical;
  }

  .radio-group {
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
  }

  .radio-option {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    padding: 0.8rem;
    background: #F0F8FF;
    border-radius: 8px;
    transition: all 0.3s ease;
    border: 1px solid #E6F7FF;
  }

  .radio-option:hover {
    background: #E6F7FF;
  }

  .radio-option span {
    font-weight: normal;
  }

  .submit-button {
    width: 100%;
    padding: 1rem;
    background: linear-gradient(to right, #5AA2D1, #4682B4);
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 0.5rem;
    box-shadow: 0 2px 5px rgba(70, 130, 180, 0.2);
  }

  .submit-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 10px rgba(70, 130, 180, 0.3);
    background: linear-gradient(to right, #4682B4, #3A7CA5);
  }

  [style="display:none"] {
    display: none !important;
  }

  @media (max-width: 480px) {
    .confirmation-section {
      padding: 1rem;
    }
    
    .event-card {
      flex-direction: column;
      text-align: center;
    }
  }
</style>